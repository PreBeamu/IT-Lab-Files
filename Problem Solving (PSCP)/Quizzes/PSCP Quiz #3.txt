ใน module builtin มี function อยู่ function หนึ่ง ซึ่งสามารถแปลงเลขฐานสิบ เป็นเลขฐานแปดในรูปของ str ได้ เช่น 10 แปลงเป็น '012'
-> oct

รับค่าเข้ามา 5 ค่าแล้วนำค่ามาหาค่าเฉลี่ย โดยค่าที่รับเข้ามาจะไม่เป็นเลขติดลบ แสดผลค่าเฉลี่ย
->  x = [0,0,0,0,0]
    for i in range(5):
        x[i] = int(input())

    print(sum(x)/len(x))

ใน module builtins มี function อยู่ function หนึ่ง ซึ่งสามารถปัดค่าเศษขึ้น หรือลง ได้ เช่นจาก 10.5 ปัดเป็น 11.0 และ 10.4 ปัดลงเป็น 10.0 ได้เป็นต้น
Hint ให้พิมพ์ help( __ builtins __ ) ที่ idle เพื่อดูรายชื่อ function ที่มีทั้งหมดของ built-in module
-> round

a,b,c จะต้องมีค่าเป็นอะไรบ้าง ตามลำดับ ถึงจะให้คำตอบออกมาเป็น True
-> True False True

หาพื้นที่ของและความยาวเส้นรอบวงของวงกลม และแสดงผลลัพธ์
->  import math
    diameter = 10
    print(round(math.pi*((diameter/2)**2),2))
    print(round(2*math.pi*(diameter/2),2))

ถ้าต้องการเขียนใหม่ให้ได้ผลแบบเดียวกันกับด้านบน จะต้องเติมอะไรลงช่องว่าง (เพียง statement เดียว บรรทัดเดียว และห้ามใช้ ­else หรือ elif)
-> return False

ตัวแปรในฟังก์ชั่น
-> parameter

___________ คือข้อความที่อธิบายการทำงานของโมดูล หรือของฟังก์ชัน โดยข้อความจะถูกล้อมรอบด้วย ''' (triple single quotes) หรือ """ (triple double quotes) ก็ได้ (ตอบเป็นเอกพจน์)
-> docstring

ถ้าต้องการตรวจสอบว่า x เป็นเลขคี่ หรือเลขคู่ ต้องใช้เครื่องหมายดำเนินการทางคณิตศาสตร์ตัวใดเพื่อให้โค้ดดังกล่าวสมบูรณ์
-> เติม %

ใน Python บรรดทัดที่มีเครื่องหมาย = (เท่ากับ) จะถูกเรียกว่า
-> assignment statement

ใน module math มี function อยู่ function หนึ่ง ซึ่งสามารถปัดค่าเศษลงได้ เช่นจาก 10.5 ปัดเป็น 10.0 ได้เป็นต้น
-> math.floor

ถ้า x มากกว่าหรือเท่ากับ 20 เป็นจริง ให้ print "True" จะเขียนเป็นเงื่อนไขตามด้านล่างนี้ได้อย่างไร
-> เติม not

การนำ ข้อความ (str) มาต่อกับ ข้อความ (str) ด้วยการใช้เครื่องหมาย + (บวก) เรียกว่า String
-> Concatenation

ให้เรียงลำดับให้ถูกต้องจากสิ่งที่ต้องทำก่อนสุด ไปยังหลังสุด ตามหลักการแก้ปัญหาของ Polya
-> 3 2 1 4