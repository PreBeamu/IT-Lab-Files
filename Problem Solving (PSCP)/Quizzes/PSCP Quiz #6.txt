จากโค้ดด้านล่าง ใน for loop หากเราไม่ได้ใช้ตัวแปรที่ได้จากการ traverse (running variable) เราต้องใส่อักษรใดลงไปแทน เพื่อให้ถูกต้องตามหลัก PEP-8
ต้องใส่เครื่่องหมาย/อักษรใด แทนเครื่องหมาย ... ด้านล่าง?
-> _

ชื่อตัวแปร local และชื่อฟังก์ชั่นควรเขียนตามหลักการตั้งชื่อแบบใดเพื่อให้ถูกต้องตามหลัก PEP-8
-> snake case

ข้อใดถูกต้อง
-> ใช้ตัวแปรยาวสื่อความหมายได้

ใน 1 บรรทัดควรมีไม่เกินกี่ตัวอักษร
-> 79

Docstring ต้องใส่ไว้ตรงไหนของโค้ด
-> ด้านบนสุด, ใต้ฟังก์ชั่นทุกตัว

Global/Constant variable
-> นอกฟังก์ชั่น, NAME

ข้อใดบ้างที่ถูกต้องตามหลัก PEP-8
->  * ต้องมีการเว้นบรรทัดสุดท้ายในไฟล์ Python
    * ใช้ตัวแปรยาวสื่อความหมายได้
    * ค่าคงที่ (constant) ที่อยู่นอกฟังก์ชัน ต้องตั้งชื่อด้วยตัวพิมพ์ใหญ่ทั้งหมด
    * เมื่อใช้ if จะต้องใส่วงเล็บครอบ expression

ต้องลบและแก้ไขบรรทัดใดเพื่อให้ถูกต้องตามหลัก PEP-8
-> 6 7
 
PEP-8 แนะนำให้ import module อย่างไร
-> import ทีละบรรทัด เช่น import os บรรทัดหนึ่ง import sys อีกบรรทัด

จากโค้ดนี้มีกี่จุดที่ไม่ถูกต้องตามหลัก PEP-8
-> 4